import type { FormBuilderOptions } from "@mfb/types";

const options: FormBuilderOptions = {
  alwaysRenderField: false,
  dependencyShouldReset: true,
  extraConditions: {},
  genDefaultValues: true,
};

export { options };
